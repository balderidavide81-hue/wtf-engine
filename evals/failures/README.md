# Production failure library

Every meaningful production/editorial failure should become a permanent regression case.

Examples:
- boring-but-weird false positive;
- unsupported claim promoted to fact;
- ambiguous Predict resolution;
- sensitive story incorrectly gamified;
- duplicate story treated as new.

Fix the system and rerun the suite rather than growing the prompt with one-off patches.
